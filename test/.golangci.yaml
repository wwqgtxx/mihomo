linters:
  disable-all: true
  enable:
    - gofumpt
    - govet
    - gci
    - staticcheck

linters-settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/metacubex/mihomo)
  staticcheck:
    go: '1.20'
